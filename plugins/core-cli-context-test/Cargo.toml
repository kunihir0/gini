[package]
name = "core-cli-context-test"
version = "0.1.0"
edition = "2021"

[dependencies]
gini-core = { path = "../../crates/gini-core" }
log = "0.4"
async-trait = "0.1"

[lib]
crate-type = ["cdylib", "rlib"]